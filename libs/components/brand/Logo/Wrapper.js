import styled from 'styled-components';

export default styled.div`
  background-color: var(--color-brand);
  overflow: hidden;
  position: relative;
  cursor: pointer;
  padding: 10px;
  margin: auto;
`;
