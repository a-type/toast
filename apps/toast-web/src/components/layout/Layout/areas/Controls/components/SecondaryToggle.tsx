import styled from 'styled-components';

const SecondaryToggle = styled.button`
  border: 0;
  background: var(--color-brand-dark);
  font-family: var(--font-default);
  width: 60px;
`;

export default SecondaryToggle;
