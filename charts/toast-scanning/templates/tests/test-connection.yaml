apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "toast-scanning.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "toast-scanning.name" . }}
    helm.sh/chart: {{ include "toast-scanning.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "toast-scanning.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
