apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev-neo4j.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "dev-neo4j.name" . }}
    helm.sh/chart: {{ include "dev-neo4j.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dev-neo4j.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
