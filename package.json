{
  "name": "toast",
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.11.0",
    "lint-staged": "^7.1.1",
    "prettier": "^1.12.1"
  },
  "scripts": {
    "precommit": "lint-staged",
    "start": "node ./scripts/start.js"
  },
  "lint-staged": {
    "*.{js,json,css,md}": ["prettier --write", "git add"]
  }
}
