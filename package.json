{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.15.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "ci:test": "lerna exec npm run ci:test",
    "ci:build": "lerna exec npm run ci:build"
  }
}
